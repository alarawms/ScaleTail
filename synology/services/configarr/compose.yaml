services:
# Make sure you have updated/checked the .env file with the correct variables. 
# All the ${ xx } need to be defined there.
  # Tailscale Sidecar Configuration
  tailscale:
    image: tailscale/tailscale:latest # Image to be used
    container_name: tailscale-configarr  # Name for local container management
    hostname: configarr  # Name used within your Tailscale environment
    environment:
    - TS_AUTHKEY=${TS_AUTHKEY}
    - TS_STATE_DIR=/var/lib/tailscale
    - TS_USERSPACE=true
    - TS_ENABLE_HEALTH_CHECK=true                # Enable healthcheck endpoint: "/healthz"
    - TS_LOCAL_ADDR_PORT=127.0.0.1:41234         # The <addr>:<port> for the healthz endpoint
      #- TS_ACCEPT_DNS=true # Uncomment when using MagicDNS
    - TS_AUTH_ONCE=true
    volumes:
    - ${DATA_ROOT:-/volume1/docker}/configarr/ts/state:/var/lib/tailscale # Tailscale requirement - you may need to change the path
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://127.0.0.1:41234/healthz"] # Check Tailscale has a Tailnet IP and is operational
      interval: 1m # How often to perform the check
      timeout: 10s # Time to wait for the check to succeed
      retries: 3 # Number of retries before marking as unhealthy
      start_period: 10s # Time to wait before starting health checks
    restart: always

  # ${SERVICE}
  application:
    image: ghcr.io/raydak-labs/configarr:latest # Image to be used
    network_mode: service:tailscale # Sidecar configuration to route ${SERVICE} through Tailscale
    container_name: app-configarr  # Name for local container management
    environment:
    - TZ=${TZ:-UTC}
    volumes:
    - ${DATA_ROOT:-/volume1/docker}/configarr/configarr-data/config:/app/config
    - ${PWD}/configarr-data/dockerrepos:/app/repos
    depends_on:
      tailscale:
        condition: service_healthy
    # healthcheck:
    #   test: ["CMD", "pgrep", "-f", "${SERVICE}"] # Check if ${SERVICE} process is running
    #   interval: 1m # How often to perform the check
    #   timeout: 10s # Time to wait for the check to succeed
    #   retries: 3 # Number of retries before marking as unhealthy
    #   start_period: 30s # Time to wait before starting health checks
    restart: never


    labels:
    - com.portainer.stack=configarr
    - com.portainer.category=scaletail
